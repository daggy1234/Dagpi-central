{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "dagpi-test",
            "database": "${workspaceFolder:dagpi-centrak}/dagpi.db"
        },
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "dagpi-test",
            "database": "${workspaceFolder:dagpi-centrak}/dagpi.db"
        },
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:dagpi-centrak}/dagpi.db",
            "name": "dagpi-data-sqlite"
        }
    ],
    "sqltools.useNodeRuntime": true
}